/* Ruby Aguilar
October 10, 2017
Lab 3.1 Conditions: The purpose of this lab is to create a progrsm that determines the best cell phone plan based on usage */

#include <iostream> 
using namespace std;


int main() 
{ 
    num_phone = 0;
    num_tablet = 0;
    data_amount = 0;
    plan_price[6];
    
    cout << " Enter number of phones. " << endl;
    cin >> num_phone;

    cout << " Enter number of tablets. " << endl;
    cin >> num_tablet;
    
    cout << " Enter the amount of data/data plan needed. " << endl;
    cin >> data_plan;
    
   int CalculateATTPlusPlan(int phones, int tablets, int data)
   {
       int plan price =0;
       int lines_needed = tablets + phones;
       
       if (lines_needed == 1)
       {
           plan_price = 90;
       }
       else if  (lines_needed == 2)
       {
           plan_price = 145;
       }
       else if ((lines_needed > 2) && (lines_needed <= 10))
       {
           
       }
       }
       }
   }
   
    
    

}